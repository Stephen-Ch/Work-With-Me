# Session Coach Pivot Status

- Active product flow is Landing → age confirmation → setup → six questions → result → copy → start over.
- Output is a short Session Coach block pasted at the start of a new AI conversation.
- The result screen is copy-first; download is no longer the primary path.
- Generated blocks are validated by unit tests for six controls, guardrails, forbidden jargon, and size targets.
- Playwright smoke coverage now follows the active session-coach path, not the retired Rawls routes.