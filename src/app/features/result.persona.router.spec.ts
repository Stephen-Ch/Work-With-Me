// V1 persona router spec — retired in V2 (no persona routing in V2).
describe('Result Persona Preview (retired)', () => {
  it('placeholder — persona routing retired in V2', () => {
    expect(true).toBe(true);
  });
});
