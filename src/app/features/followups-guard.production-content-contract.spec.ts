// V1 followupsGuard production contract — retired in V2.
describe('followupsGuard production contract (retired)', () => {
  it('placeholder — guard retired in V2', () => {
    expect(true).toBe(true);
  });
});
